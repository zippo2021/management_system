{% extends 'base.html' %}
{% block content %}
<table>
{% for each in users  %}
<tr>
    <td>
        {{ each.UserData }}
        <a href="{% url 'view_profile' each.user.data.id %}">{{ each.user.data.last_name }} {{ each.user.data.first_name }} {{ each.user.data.middle_name }}</a>        
    </td>
    <td>
    {{ each.status }}
    </td>
    <td>
       {% if accept  %} 
       {% if each.status != 'Accepted'  %} 
        <a href="{% url 'events_accept_request' eid each.user.data.id %}" > Одобрить</a>
       {% else %}
        <a href="{% url 'decline_request' eid each.user.data.id %}" > Отказать</a>
       {% endif %}
    </td>
    
    
</tr>
{% endfor %}
</table>
{% endblock %}
