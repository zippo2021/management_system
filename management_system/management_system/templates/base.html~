<!DOCTYPE html>
{% load i18n %}
{% load staticfiles %}
<html>
<head>
	<link rel="stylesheet" href="{% static "markup.css" %}">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.11.1/jquery-ui.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"></script>
    <script src="http://malsup.github.com/jquery.form.js"></script>
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="//ajax.googleapis.com/ajax/libs/jqueryui/1.11.1/themes/smoothness/jquery-ui.css">
	{% block extrahead %}{% endblock %}
</head>

<body>
	<div id="container">
		<div id="top_menu">
		Top menu:
		{% if user.UserData %}
			you are <font color="blue">{{ user.username }} </font>
			<font color="red">
			{% if user.UserData.is_teacher %}
				Teacher!
			{% endif %}
			{% if user.UserData.is_event_worker %}
				EventWorker!
			{% endif %}
			{% if user.UserData.is_mentor %}
				Mentor!
			{% endif %}
			{% if user.UserData.is_observer %}
				Observer!
			{% endif %}
			{% if user.UserData.RegularUser %}
				RegularUser!
			{% endif %}
			</font>
		{% endif %}
		</div>
		
		<div id="org_info">
			<div id="org_title">
				{{ organisation_settings.title }}
			</div>
		</div>
		
		<div id="navigation">
		Main menu
		</div>

		<div id="sidebar">
			{% block sidebar %}
			<ul>
				Sidebar
			</ul>
			{% endblock %}
		</div>

		<div id="content">
			{% block content %}{% endblock %}
		</div>

		<div id="clear">
		</div>

		<div id="footer">
			Footer
		</div>

	</div>	
